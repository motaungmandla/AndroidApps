name: lesotho_mineral_atlas
description: Alliance Demo Hub - Motaung Mandla Portfolio
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # ✅ NO EXTERNAL PACKAGES - Pure Flutter SDK only!

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
