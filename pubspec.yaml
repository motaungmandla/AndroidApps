name: "Motaung demo"
description: "Motaung Mandla Portfolio"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # ✅ State Management
  provider: ^6.1.1
  # ✅ Launch external URLs
  url_launcher: ^6.2.4
  # ✅ Native sharing
  share_plus: ^7.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
